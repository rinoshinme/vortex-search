MODEL:
  TYPE: "CLIP"
  NAME: "ViT-B/32"
  WEIGHTS: ""
  THRESHOLD: 0.2

FAISS:
  FEATURE_DIM: 512
  INDEX_TYPE: "Flat"
  INDEX_PATH: "data/clip/clip.index"

SQLITE:
  TABLE_NAME: "clip"
  DB_PATH: "data/clip/clip.db"

MEDIA:
  MEDIA_ROOT: "D:/data/image_search"
